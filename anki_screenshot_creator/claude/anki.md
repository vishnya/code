Read `~/code/anki_screenshot_creator/CONTEXT.md` for project context, then read `~/anki_watcher.py` and `~/.hammerspoon/init.lua` to see the current state of the code. Summarize architecture and current status in 5 bullet points, then ask what we're working on today.

When the session is done, update `~/code/anki_screenshot_creator/CONTEXT.md` — specifically the "Recent Changes" section — to reflect what changed, so the next session starts with accurate context.
